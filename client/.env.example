# Client environment example
# Copy to .env and fill in values

# API base URL (should include /api when pointing directly to server)
# Example local dev with Vite proxy: omit to use '/api'
# Example production: https://api.yourdomain.com/api
VITE_API_URL=
